---
title: Authentication Template
description: "This endpoint is for authentication templates, use this endpoint to send a authentication template."
api: "POST /whatsapp"
playground: true
---

This endpoint is designed for templates without complex headers but supports parameters for customization. Simply plug in and configure your parameters for easy and efficient use.

#### Request Body Parameters

<ParamField path="from" type="string" required>
  The number registered with WhatsApp business from which the message is to be
  sent. Ensure that the country code is prefixed to the number. For example, if
  the number is in the India, the format would be: +91XXXXXXXXXX
</ParamField>

<ParamField path="templateName" type="string" required>
  This template name contains only lowercase alphanumeric characters and
  underscores.
</ParamField>

<ParamField path="to" type="string" required>
  Recipient's MSISDN
</ParamField>

<ParamField path="type" type="string" required>
  WhatsApp message templates type
</ParamField>

<ParamField path="otp" type="string" required>
  WhatsApp OTP
</ParamField>

<ParamField path="campaignName" type="string">
  The WhatsApp campaign name refers to the specific name or title given to a
  campaign conducted through WhatsApp
</ParamField>

<CodeGroup>

```json SampleRequest.json
curl -G - /v1/whatsapp\
-H 'apikey':'string'
-H "Content-Type: application/json" \
-d '{
    "from": "91XXXXXXXXXX ",
    "campaignName": "api-test",
    "to": "91XXXXXXXXXX",
    "templateName": "template_name",
    "otp": "XXXX",
    "type": "template",
    "language": {
        "code": "en"
    }
}
```

```json SampleResponse.json
{
  "id": "58d63c02-XXXX-XXXX-XXXX-XXXXbb39453",
  "data": [
    {
      "recipient": "91XXXXXXXXXX",
      "messageId": "58d63c02-XXXXX-XXXXX-XXXXX-XXXX9bb39453:1"
    }
  ],
  "totalCount": 1,
  "message": "Message Sent Successfully!",
  "error": null
}
```

</CodeGroup>
